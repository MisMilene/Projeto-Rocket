function toggleMode() {
   const html=document.documentElement
  
   /*isso faz o botao mover e faz a tela mudar comando de javascript*/
   /*
   if(html.ClassList.constains("ligth")){
   html.ClassList.remove("ligth");
   }else {
  
    html.classList.add("ligth")
   }

   /* ou essa funçao pode ser feita de outra forma */
          html.classList.toggle("ligth")
    
}
